@attribute [Stories("Icons/SettingsIcon")]

<Stories TComponent="SettingsIcon">
    <Story Name="Primary" Layout="typeof(DarkLayout)">
        <Template>
            <SettingsIcon Size="80px" @attributes="context.Args" />
        </Template>
    </Story>
    <Story Name="Secondary" Layout="typeof(LightLayout)">
        <Arguments>
            <Arg For="c => c.ComponentStyle" Value="ComponentStyle.Secondary" />
        </Arguments>
        <Template>
            <SettingsIcon Size="80px" @attributes="context.Args"/>
        </Template>
    </Story>
    <Story Name="Small" Layout="typeof(DarkLayout)">
        <Template>
            <SettingsIcon Size="40px" @attributes="context.Args" />
        </Template>
    </Story>
</Stories>