@attribute [Stories("Icons/CheckIcon")]

<Stories TComponent="CheckIcon">
    <Story Name="Primary" Layout="typeof(DarkLayout)">
        <Template>
            <CheckIcon Size="80px" @attributes="context.Args" />
        </Template>
    </Story>
    <Story Name="Secondary" Layout="typeof(LightLayout)">
        <Arguments>
            <Arg For="c => c.ComponentStyle" Value="ComponentStyle.Secondary" />
        </Arguments>
        <Template>
            <CheckIcon Size="80px" @attributes="context.Args"/>
        </Template>
    </Story>
    <Story Name="Small" Layout="typeof(DarkLayout)">
        <Template>
            <CheckIcon Size="40px" @attributes="context.Args" />
        </Template>
    </Story>
</Stories>