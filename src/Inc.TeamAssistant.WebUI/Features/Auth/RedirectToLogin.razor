@inject TeamAssistantRouter TeamAssistantRouter

@code {
    private string CurrentUrl => TeamAssistantRouter.CurrentRelativeUrl;
    
    protected override void OnInitialized()
    {
        TeamAssistantRouter.NavigateTo($"/login?returnUrl={CurrentUrl}");
    }
}