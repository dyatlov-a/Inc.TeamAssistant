@page "/survey/{RoomId:guid}"
@page "/ru/survey/{RoomId:guid}"

@inject NavRouter NavRouter

<Survey RoomId="RoomId" LinkFactory="@(v => NavRouter.CreateRoute(v))" />

@code {
    [Parameter, EditorRequired]
    public Guid RoomId { get; set; }
}